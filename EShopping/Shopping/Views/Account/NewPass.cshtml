@model AppUserModel
@{
	ViewData["title"] = "New pass page";
}
<section>
	<!--form-->
	<div class="container">
		<div class="row">
			<div class="col-sm-12 col-sm-offset-1">
				<div class="login-form">
					<!--login form-->
					<h2>Reset a new password</h2>
					<div asp-validation-summary="ModelOnly" class="text-danger" />
					<form asp-action="UpdateNewPass" asp-controller="Account">
						<input type="hidden" asp-for="Token" value="@ViewBag.Token"/>
						<div>
							<label>Email</label>
							<input readonly asp-for="Email" value="@ViewBag.Email" class="form-group" />
							<span asp-validation-for="Email" class="text-danger" />
						</div>
						<div>
							<label>New Password</label>
							<input asp-for="PasswordHash" class="form-group" />
							<span asp-validation-for="PasswordHash" class="text-danger" />
						</div>
						<button type="submit" class="btn btn-default">Send</button>
						<a asp-action="create" asp-controller="Account">Register</a>
					</form>
				</div><!--/login form-->
			</div>

		</div>
	</div>
</section><!--/form-->
@section Scripts {
	@{
		await Html.RenderPartialAsync("_ValidationScriptsPartial");
	}
}